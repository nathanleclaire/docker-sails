db:
    image: postgres
    volumes:
     - /var/lib/postgresql/data:/var/lib/postgresql/data
web:
    build: .
    volumes_from:
     - volume_container
    ports:
     - "1337:1337"
    links:
     - db
